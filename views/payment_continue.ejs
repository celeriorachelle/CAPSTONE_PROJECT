<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Continue Payment | Everlasting Peace Memorial Park</title>
  <link rel="stylesheet" href="/stylesheets/userdashboard.css">
  <link rel="stylesheet" href="/stylesheets/payment.css">
</head>
<body>
  <div class="container">
    <div class="payment-card">
      <h2>Continue Payment</h2>
      <p>Booking Reference: <strong><%= booking.booking_id %></strong></p>

      <div class="payment-details">
        <p><strong>Plot Number:</strong> <%= booking.plot_number %></p>
        <p><strong>Location:</strong> <%= booking.location %></p>
        <p><strong>Total Price:</strong> ₱<%= booking.total_price.toLocaleString() %></p>
        <p><strong>Payment Type:</strong> <%= booking.payment_type %></p>
        <p><strong>Amount to Pay (Monthly):</strong> ₱
  <%= booking.monthly_amount != null ? booking.monthly_amount.toLocaleString() : '0.00' %>
</p>

        <p><strong>User Email:</strong> <%= booking.user_email %></p>
      </div>

      <form id="installmentForm" action="/payment/installment-checkout" method="POST">
        <input type="hidden" name="booking_id" value="<%= booking.booking_id %>">
        <input type="hidden" name="user_id" value="<%= booking.user_id %>">
        <input type="hidden" name="plot_id" value="<%= booking.plot_id %>">
        <input type="hidden" name="user_email" value="<%= booking.user_email %>">
        <input type="hidden" name="option" value="downpayment">
        <input type="hidden" name="payment_type" value="downpayment">

        <div class="payment-actions">
          <button type="submit" class="pay-btn">Proceed to Payment</button>
          <a href="/payment_history" class="cancel-btn">Cancel</a>
        </div>
      </form>
    </div>
  </div>

  <script>
    document.getElementById('installmentForm').addEventListener('submit', async function(e) {
      e.preventDefault();
      const form = e.target;
      const formData = new FormData(form);
      // Debug: log user_email value
      console.log('user_email:', formData.get('user_email'));
      const response = await fetch(form.action, {
        method: 'POST',
        body: formData
      });
      const result = await response.json();
      if (result.url) {
        window.location.href = result.url;
      } else {
        alert(result.error || 'Payment initiation failed.');
      }
    });
  </script>

  <style>
    body {
      background: #f7f9fc;
      font-family: 'Poppins', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .payment-card {
      background: white;
      padding: 2rem;
      border-radius: 20px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
      width: 420px;
    }

    h2 {
      text-align: center;
      color: #333;
      margin-bottom: 1rem;
    }

    .payment-details p {
      margin: 5px 0;
      color: #555;
    }

    label {
      display: block;
      margin-top: 1rem;
      color: #333;
      font-weight: 500;
    }

    input {
      width: 100%;
      padding: 10px;
      border-radius: 8px;
      border: 1px solid #ccc;
      margin-top: 5px;
    }

    .payment-actions {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 1.5rem;
    }

    .pay-btn {
      background: #4caf50;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 8px;
      cursor: pointer;
      transition: 0.3s;
    }

    .pay-btn:hover {
      background: #43a047;
    }

    .cancel-btn {
      text-decoration: none;
      color: #777;
      font-weight: 500;
      transition: 0.3s;
    }

    .cancel-btn:hover {
      color: #333;
    }
  </style>
</body>
</html>