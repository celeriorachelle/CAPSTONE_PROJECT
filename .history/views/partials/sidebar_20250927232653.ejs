<div class="sidebar" id="sidebar">
  <div class="sidebar-header">
    <button class="toggle-btn" id="toggleBtn">
      <span></span>
      <span></span>
      <span></span>
    </button>
    <h2>Everlasting Peace<br>Memorial Park Cemetery</h2>
  </div>
  <nav>
    <a href="/" class="active">Home</a>
    <a href="/viewbooking">View Bookings</a>
    <a href="/book">Make a Reservation (Plot/Services)</a>
    <a href="/payments">Payment History</a>
    <a href="/notifications">Notifications</a>
    <a href="/support">Support</a>
  </nav>
</div>

<style>
/* SIDEBAR */
.sidebar {
  width: 250px;
  background: #101b0d;
  color: white;
  display: flex;
  flex-direction: column;
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  transition: transform 0.3s ease;
  transform: translateX(0);
  z-index: 1000;
  box-shadow: 4px 0 10px rgba(0,0,0,0.2);
}

.sidebar.collapsed {
  transform: translateX(-100%);
}

/* HEADER */
.sidebar-header {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  gap: 10px;
  padding: 16px 20px;
  border-bottom: 1px solid rgba(203, 203, 203, 0.25);
  font-family: 'CMU Serif', serif;
}

.sidebar-header h2 {
  font-size: 15px;
  font-weight: 600;
  line-height: 1.4;
  color: #f5f5f5;
}

/* NAVIGATION */
.sidebar nav {
  margin-top: 30px;
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.sidebar nav a {
  padding: 12px 20px;
  text-decoration: none;
  color: #f1f1f1;
  border-radius: 6px;
  margin: 0 20px;
  font-weight: 400;
  transition: background 0.2s ease;
  font-family: 'Poppins', sans-serif;
  font-size: 14px;
}

.sidebar nav a:hover {
  background: rgba(145, 159, 101, 0.4);
}

.sidebar nav a.active {
  background: #28a745;
  font-weight: 600;
  color: #fff;
}

/* TOGGLE BUTTON (hamburger inside sidebar) */
.toggle-btn {
  background: transparent;
  border: none;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  cursor: pointer;
  gap: 5px;
  padding: 6px;
}

.toggle-btn span {
  display: block;
  width: 20px;
  height: 2px;
  background: #fff;
  border-radius: 2px;
  transition: all 0.3s ease;
}

.toggle-btn:hover span {
  background: #28a745;
}
</style>

<script>
const toggleBtn = document.getElementById("toggleBtn");
const sidebar = document.getElementById("sidebar");

toggleBtn.addEventListener("click", () => {
  sidebar.classList.toggle("collapsed");
});
</script>

<!-- Google Fonts -->
<link href="https://fonts.googleapis.com/css2?family=CMU+Serif:wght@400;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
